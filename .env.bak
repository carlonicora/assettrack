# =============================================================================
# ASSETTRACK MONOREPO - CENTRALIZED ENVIRONMENT VARIABLES
# =============================================================================
# This file contains ALL environment variables for the entire monorepo.
# Variables are organized by category and marked with their consuming app(s).
# =============================================================================

# =============================================================================
# API CONFIGURATION (API)
# =============================================================================
API_URL="http://localhost:3400/"
API_PORT=3400

# =============================================================================
# WEB APP CONFIGURATION (WEB)
# =============================================================================
APP_URL="http://localhost:3401/"
NEXT_PUBLIC_API_URL="http://localhost:3400/"
NEXT_PUBLIC_ADDRESS="http://localhost:3401"
PORT=3401

# =============================================================================
# DATABASE - NEO4J (API)
# =============================================================================
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=password
NEO4J_DATABASE=assettrack

# =============================================================================
# CACHE & QUEUE - REDIS (API)
# =============================================================================
REDIS_HOST=localhost
REDIS_PASSWORD=
REDIS_PORT=6379
REDIS_USERNAME=
QUEUE=assettrack

# =============================================================================
# CACHE CONFIGURATION (API)
# =============================================================================
CACHE_ENABLED=false
CACHE_DEFAULT_TTL=600
CACHE_SKIP_PATTERNS=""

# =============================================================================
# AUTHENTICATION - JWT (API)
# =============================================================================
JWT_SECRET="assettrack_SECRET"
JWT_EXPIRES_IN="1h"

# =============================================================================
# WEB PUSH - VAPID (API & WEB)
# =============================================================================
VAPID_PUBLIC_KEY=""
VAPID_PRIVATE_KEY=""
VAPID_EMAIL=""

# Web-specific VAPID public key (for client-side use)
NEXT_PUBLIC_VAPID_PUBLIC_KEY="BNM4LS51SC61s2XBTv5vRUBhaq67gZmAsCQRkCu-dzQShOUm_D_QeopCJQS08kN55SBtdjiKzciJHjLPSzdJJ2Y"

# =============================================================================
# EMAIL CONFIGURATION (API)
# =============================================================================
EMAIL_PROVIDER="smtp"
EMAIL_API_KEY=
EMAIL_FROM="Avvocato 360 AI<info@phlow.com>"
EMAIL_HOST="smtp-relay.brevo.com"
EMAIL_PORT="587"
EMAIL_SECURE="false"
EMAIL_USERNAME="8b73d7001@smtp-brevo.com"
EMAIL_PASSWORD="j2CTUSkAmcMvx5fs"

# =============================================================================
# LOGGING - LOKI (API)
# =============================================================================
LOKI_ENABLED=true
LOKI_HOST=http://localhost:3100
# LOKI_USERNAME=your_username
# LOKI_PASSWORD=your_password
LOKI_APP_LABEL=assettrack-api

# =============================================================================
# TRACING - TEMPO (API)
# =============================================================================
TEMPO_ENABLED=true
TEMPO_ENDPOINT=http://localhost:4318/v1/traces
TEMPO_SERVICE_NAME=assettrack-api
TEMPO_SERVICE_VERSION=1.0.0

# =============================================================================
# OBJECT STORAGE - S3 / MINIO (API)
# =============================================================================
S3_TYPE="minio"
S3_ENDPOINT="http://localhost:9000/"
S3_BUCKET="assettrack"
S3_ACCESS_KEY_ID="minioadmin"
S3_SECRET_ACCESS_KEY="minioadmin"
S3_REGION=""

# =============================================================================
# RATE LIMITING / THROTTLER (API)
# =============================================================================
RATE_LIMIT_ENABLED=true
RATE_LIMIT_TTL=60000
RATE_LIMIT_REQUESTS=1000
IP_RATE_LIMIT_REQUESTS=200

# =============================================================================
# CORS CONFIGURATION (API)
# =============================================================================
CORS_ORIGINS=http://localhost:3401
CORS_ORIGIN_PATTERNS=
CORS_CREDENTIALS=true
CORS_METHODS=GET,HEAD,PUT,PATCH,POST,DELETE,OPTIONS
CORS_ALLOWED_HEADERS=Content-Type,Authorization,X-Requested-With,x-language,x-companyid
CORS_MAX_AGE=86400
CORS_PREFLIGHT_CONTINUE=false
CORS_OPTIONS_SUCCESS_STATUS=204
CORS_LOG_VIOLATIONS=false

# =============================================================================
# UPC DATABASE (API)
# =============================================================================
UPC_ENABLED=true
#UPC_API_KEY=B9D564D4B7573FAF0DC208F6398EF828

# =============================================================================
# IMAGE SOURCES (WEB)
# =============================================================================
IMAGE_SOURCES=https://phlow-test.lon1.digitaloceanspaces.com,https://phlow-dev.lon1.digitaloceanspaces.com,https://phlow-internal.lon1.digitaloceanspaces.com,https://phlowtest.blob.core.windows.net,http://localhost:9000
